# create a resource group
az group create --name my-bicep-rg --location francecentral

# Deploy an ARM template 
az deployment group create  \
--resource-group my-bicep-rg \
--template-file main.json
